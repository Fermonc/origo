'use client';

// Placeholder for future map implementation
export default function MapPage() {
    return (
        <div style={{
            height: '100vh',
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            padding: '20px',
            textAlign: 'center'
        }}>
            <h1>Mapa no disponible</h1>
            <p>Estamos trabajando en una nueva experiencia de mapa.</p>
            <a href="/" style={{ marginTop: '20px', textDecoration: 'underline' }}>Volver al inicio</a>
        </div>
    );
}
